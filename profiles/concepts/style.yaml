id: style
concept:
  type: style

tagger:
  stable_backend: wd_eva02_large_v3
  dual_enabled: false
  challenger_backend: null
  conflict_delta: 0.35

identity_check:
  enabled: false
  method: null

caption:
  trigger_position: first
  describe_subject: true
  describe_clothing: true
  describe_pose: true
  describe_scene: true
  describe_objects: true
  describe_composition: true
  describe_lighting_context: true
  suppress_style_descriptors: true
  blacklist:
    - artist_name
    - signature
    - watermark

distribution:
  tags:
    portrait: [portrait, close-up, close_up, face]
    full_body: [full_body]
    single_person: [1girl, 1boy, solo]
    multiple_people: [2girls, 2boys, multiple_girls, multiple_boys, group]
    female: [1girl, female]
    male: [1boy, male]
    landscape: [landscape, scenery, no_humans]
    indoor: [indoors]
    outdoor: [outdoors]
    day: [day]
    night: [night]
    simple_background: [simple_background]
    complex_background: [detailed_background, complex_background]
  bias_warning:
    dominant_subject_fraction: 0.8
    portrait_fraction: 0.8
    simple_background_fraction: 0.8
    minimum_multiple_people_fraction: 0.05

# These guardrails are intentionally under `limits`, which is already part of the
# preflight/train fingerprint. One concentrated axis is only a warning; automatic
# blocking requires severe subject concentration plus severe composition/background
# concentration, reducing false positives for intentionally portrait-oriented styles.
limits:
  style_bias:
    minimum_images: 12
    extreme_fraction: 0.95

evaluation:
  identity: false
  leakage: true
  strengths: [0.4, 0.6, 0.8, 1.0]
  screening_strengths: [0.6, 0.8, 1.0]
  screening_prompts:
    - female portrait
    - male portrait
    - landscape
  prompts:
    - female portrait
    - male portrait
    - full body
    - two characters
    - animal
    - landscape
    - interior
    - city
    - nature
    - day
    - night
    - action scene
    - simple composition
    - complex composition
